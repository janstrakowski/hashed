target
SPEC.html
/hb
# The same binary on Windows, plus the test executable `odin test` drops in the
# working directory when it isn't told to put it somewhere else. `odin build`
# also emits a .pdb next to the .exe.
/hb.exe
/hbtest.exe
/src.exe
*.pdb
.claude/
hb.wasm
.cache/
hb-threads.wasm
__pycache__/

# The DAP harness is npm, and only dap-tests/ needs it (see CLAUDE.md).
dap-tests/node_modules/
dap-tests/package-lock.json
