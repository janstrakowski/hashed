// A fixture for dap-tests: arithmetic only, so nothing here reads the
// filesystem or spawns a thread. Its value is 14, and the expression is on
// line 7 - which adapter.test.js sets a breakpoint on, so keep it there or
// keep the test in step with it.
//
// (Line 1 is a comment, which is the other half of that test.)
2 + 3 * 4
