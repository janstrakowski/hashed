{
  "name": "hashedbuild-playground",
  "private": true,
  "type": "module",
  "description": "Tests for the browser playground (docs/): the WASI shim under Node, and the page itself under headless Chrome. The interpreter itself is Odin - see CLAUDE.md.",
  "scripts": {
    "test:shim": "node scripts/playground_test.mjs",
    "test:browser": "node scripts/playground_browser_test.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
